FROM gvenzl/oracle-xe:latest

ENV ORACLE_PASSWORD=oracle123

HEALTHCHECK --interval=30s --timeout=10s --start-period=60s \
  CMD echo 'SELECT 1 FROM DUAL;' | sqlplus -s SYSTEM/${ORACLE_PASSWORD}@//localhost:1521/XE

EXPOSE 1521